vgpu-manager DRA driver deployed in namespace: {{ .Release.Namespace | quote }}
DRA resource API version in use: {{ include "vgpu-manager-dra-driver.resourceApiVersion" . | trim | quote }}

{{- if .Values.kubeletPlugin.enabled }}

vgpu-manager-dra-driver {{ .Chart.AppVersion }} kubelet-plugin (DaemonSet) has been deployed successfully!
  - NodeSelector   :  {{ .Values.kubeletPlugin.nodeSelector | toJson }}
  - DriverRoot     :  {{ .Values.kubeletPlugin.driverRoot | default "/" | quote }}
  - HealthcheckPort:  {{ .Values.kubeletPlugin.containers.plugin.healthcheckPort | default 4567 }}
  - FeatureGates   :  {{ .Values.kubeletPlugin.containers.plugin.featureGates | toJson }}
  - Metrics        :  {{ .Values.kubeletPlugin.metrics.enabled }}
  - LoggerLevel    :  {{ .Values.kubeletPlugin.containers.plugin.logLevel | default 5 }}

  NOTE: nodes are only scheduled by the DRA driver once labelled to match the nodeSelector above, e.g.:
    kubectl label node <node> vgpu-manager=dra-driver
{{- end }}

{{- if .Values.deviceClasses.enabled }}

vgpu-manager-dra-driver {{ .Chart.AppVersion }} DeviceClasses (driver: {{ .Values.deviceClasses.driverName | default "manager.nvidia.com" | quote }}):
{{- if .Values.deviceClasses.gpu.enabled }}
  - {{ .Values.deviceClasses.gpu.name | default "gpu-manager" }} (type=gpu)
{{- end }}
{{- if .Values.deviceClasses.vgpu.enabled }}
  - {{ .Values.deviceClasses.vgpu.name | default "vgpu-manager" }} (type=vgpu)
{{- end }}
{{- if .Values.deviceClasses.mig.enabled }}
  - {{ .Values.deviceClasses.mig.name | default "mig-manager" }} (type=mig)
{{- end }}
{{- if .Values.deviceClasses.vfio.enabled }}
  - {{ .Values.deviceClasses.vfio.name | default "vfio-manager" }} (type=vfio)
{{- end }}
{{- end }}

{{- if .Values.webhook.enabled }}

vgpu-manager-dra-driver {{ .Chart.AppVersion }} webhook has been deployed successfully!
  - ReplicaCount   :  {{ .Values.webhook.replicas }}
  - DraAdmission   :  enabled (always)
  - ServerBindPort :  {{ .Values.webhook.commands.serverBindPort | default 9443 }}
  - PprofBindPort  :  {{ .Values.webhook.commands.pprofBindPort | default 0 }}
  - RuntimeClass   :  {{ .Values.webhook.commands.defaultRuntimeClass | quote }}
  - NodePolicy     :  {{ .Values.webhook.commands.defaultNodePolicy | quote }}
  - DevicePolicy   :  {{ .Values.webhook.commands.defaultDevicePolicy | quote }}
  - TopologyMode   :  {{ .Values.webhook.commands.defaultTopologyMode | quote }}
  - VGPUClassName  :  {{ .Values.webhook.commands.vgpuDeviceClassName | quote }}
  - Convert2DRA    :  {{ .Values.webhook.commands.defaultConvert2Dra }}
  - CombinedClaim  :  {{ .Values.webhook.commands.combinedResourceClaim }}
  - CertManager    :  {{ .Values.webhook.certManager.enabled }}
  - LoggerLevel    :  {{ .Values.webhook.commands.logLevel }}
{{- if not .Values.webhook.certManager.enabled }}

  The webhook TLS certificate is generated by Helm (self-signed). To use cert-manager instead,
  install cert-manager and set `webhook.certManager.enabled=true`.
{{- else }}

  The webhook TLS certificate is managed by cert-manager. Ensure cert-manager is installed.
{{- end }}
{{- end }}

** Please be patient while the chart is being deployed **
