{{- if .Values.webhook.enabled -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "vgpu-manager-dra-driver.webhook" . }}-as-role
  labels:
    app.kubernetes.io/component: {{ include "vgpu-manager-dra-driver.webhook" . }}
    {{- include "vgpu-manager-dra-driver.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: {{ include "vgpu-manager-dra-driver.webhook" . }}
    namespace: {{ .Release.Namespace | quote }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "vgpu-manager-dra-driver.webhook" . }}:role
{{- end }}
