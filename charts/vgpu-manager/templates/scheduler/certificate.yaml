{{- if and .Values.scheduler.enabled .Values.scheduler.schedulerExtender.tls.enabled -}}
{{- if .Values.scheduler.schedulerExtender.tls.certManager.enabled -}}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "vgpu-manager.scheduler" . }}-cert
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app.kubernetes.io/component: {{ include "vgpu-manager.scheduler" . }}
    {{- include "vgpu-manager.labels" . | nindent 4 }}
spec:
  dnsNames:
    - {{ include "vgpu-manager.scheduler" . }}.{{ .Release.Namespace }}.svc
    - {{ include "vgpu-manager.scheduler" . }}.{{ .Release.Namespace }}.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: {{ include "vgpu-manager.scheduler" . }}-issuer
  secretName: {{ include "vgpu-manager.scheduler" . }}-secret
  duration: {{ printf "%dh" (mul .Values.scheduler.schedulerExtender.tls.certDuration 24) }}
  renewBefore: {{ printf "%dh" (mul .Values.scheduler.schedulerExtender.tls.certManager.certRenewBefore 24) }}
{{- else }}
{{- $webhookSvcName := include "vgpu-manager.scheduler" . }}
{{- $webhookSvcAltNames := list (printf "%s.%s.svc" $webhookSvcName .Release.Namespace) (printf "%s.%s.svc.cluster.local" $webhookSvcName .Release.Namespace) }}
{{- $certName := printf "%s-cert" (include "vgpu-manager.scheduler" .) }}
{{- $secretName := printf "%s-secret" (include "vgpu-manager.scheduler" .) }}
{{- $duration := int .Values.scheduler.schedulerExtender.tls.certDuration }}
{{- /*
  Self-signed TLS for the scheduler-extender endpoint (Helm-CLI path). A STABLE CA is
  persisted in the Secret and reused across upgrades (via lookup); only the leaf is
  re-issued on upgrade to refresh its validity. Plain (non-hook) resource so
  `helm uninstall` removes it cleanly.
  NOTE: lookup returns nothing during `helm template`/--dry-run/client-side GitOps;
  use the cert-manager path for those workflows.
*/ -}}
{{- $existing := lookup "v1" "Secret" .Release.Namespace $secretName }}
{{- $ca := "" }}
{{- if and $existing $existing.data (index $existing.data "ca.crt") (index $existing.data "ca.key") }}
{{- $ca = buildCustomCert (index $existing.data "ca.crt") (index $existing.data "ca.key") }}
{{- else }}
{{- $ca = genCA $certName $duration }}
{{- end }}
{{- $cert := genSignedCert (printf "%s.%s" $webhookSvcName .Release.Namespace) nil $webhookSvcAltNames $duration $ca }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "vgpu-manager.scheduler" . }}-secret
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app.kubernetes.io/component: {{ include "vgpu-manager.scheduler" . }}
    {{- include "vgpu-manager.labels" . | nindent 4 }}
type: kubernetes.io/tls
data:
  # ca.key is persisted so upgrades can reuse the same CA (stable ca.crt).
  ca.crt: {{ $ca.Cert | b64enc }}
  ca.key: {{ $ca.Key | b64enc }}
  tls.crt: {{ $cert.Cert | b64enc }}
  tls.key: {{ $cert.Key | b64enc }}
{{- end }}
{{- end }}