apiVersion: resource.k8s.io/v1
kind: ResourceClaimTemplate
metadata:
  name: fm-partition-2gpu
spec:
  spec:
    devices:
      requests:
      - name: gpus
        exactly:
          deviceClassName: vfio-manager
          allocationMode: ExactCount
          count: 2
      constraints:
      # All allocated GPUs must share the same size-2 FM partition
      - requests:
        - gpus
        matchAttribute: manager.nvidia.com/partition2
